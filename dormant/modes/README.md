---
globs: .claude/modes/**
---
# Behavioral Modes

When `~/.claude/rules/_active_mode.md` exists, it contains the active behavioral overlay.
Follow its instructions as additive rules on top of all other configuration.
Only one mode is active at a time. Use `/mode` to switch.
