{
  "session_count": 488,
  "project": "torus-framework",
  "feature": "mcp-connection-fix",
  "framework_version": "v3.0.0",
  "what_was_done": "Fixed mcp.json double-encoding (root cause of MCP failure). Added SSE health watchdog to memory_server.py (3-strike CPU/thread monitor with os.execv self-restart). Killed 28K zombies from stale Next.js container.",
  "known_issues": [
    "CRITICAL: voice-web routing — messages go to CLI instance instead of tmux pane",
    "hasAvailableSubscription can go stale — check on resume if 1M context missing",
    "TAP watchdog + fallback not wired into heartbeat loop yet",
    "test_framework.py harness: 17 pre-existing failures (gate count drift, import)",
    "ps/pgrep hang on this box — use /proc/{pid}/status directly"
  ],
  "next_steps": [
    "Verify memory MCP tools register after session restart — search_knowledge test",
    "Check startup timing log — verify FTS skip/optimize works on 17GB LanceDB",
    "DAG: verify auto-promotion fires at session end — check knowledge table for source:dag entries",
    "DAG: test /clear interception — does it create a new branch?",
    "Memory maintenance — stale purge (3232 entries)"
  ]
}
